meta {
  name: sites
  type: http
  seq: 1
}

post {
  url: {{API_URL}}/{{resource}}/{{action}}
  body: json
  auth: inherit
}

body:json {
  {
    "search": {
      "includes": [
        {
          "relation": "sites"
        }
      ]
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
